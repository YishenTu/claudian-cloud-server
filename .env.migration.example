# Use this file only with the separately invoked migration entry point.
# The migration role is distinct from the server runtime role. Offline backup
# also receives it only for immediate-predecessor terminal ID enumeration.
CLAUDIAN_CLOUD_POSTGRES_MIGRATION_URL=postgresql://claudian_cloud_migration:replace-with-migration-secret@127.0.0.1:5432/claudian_cloud
