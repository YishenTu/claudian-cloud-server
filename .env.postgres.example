# Use this file only for the PostgreSQL service.
# Keep the bootstrap credential in an external credential store.
POSTGRES_USER=claudian_cloud_bootstrap
POSTGRES_PASSWORD=replace-with-bootstrap-secret
POSTGRES_DB=postgres
